version: "3.8"

services:
  jupyter:
    image: jupyter/scipy-notebook:latest
    container_name: de-module4-jupyter
    ports:
      - "8888:8888"
    volumes:
      - ./:/home/jovyan/work
    environment:
      - JUPYTER_TOKEN=changeme
